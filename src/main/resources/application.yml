spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://122.51.104.146:3306/mp?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
logging:
  level:
    root: warn
    com.sam.dao: trace
  pattern:
    #日志格式
    console: '%p%m%n'

mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
